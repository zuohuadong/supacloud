{
    "name": "supabase-manager",
    "version": "0.0.0",
    "module": "index.ts",
    "type": "module",
    "dependencies": {
        "jsonwebtoken": "^9.0.2"
    },
    "scripts": {
        "setup": "bun run setup.ts",
        "start": "bun run index.ts",
        "build": "bun build --compile --minify --sourcemap index.ts --outfile ../bin/supacloud"
    },
    "devDependencies": {
        "@types/bun": "latest"
    },
    "peerDependencies": {
        "typescript": "^5.0.0"
    }
}